# 🔑 Создание Personal Access Token в GitLab

## Способ 1: Через веб-интерфейс (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Откройте настройки пользователя

1. Войдите в GitLab: https://gyber.org/lab
2. В правом верхнем углу нажмите на **аватар** (или иконку пользователя)
3. Выберите **"Preferences"** или **"Настройки"**

### Шаг 2: Перейдите в Access Tokens

1. В левом меню найдите раздел **"Access Tokens"** или **"Токены доступа"**
2. Или перейдите напрямую: https://gyber.org/lab/-/user_settings/personal_access_tokens

### Шаг 3: Создайте новый токен

1. Нажмите кнопку **"Add new token"** или **"Добавить новый токен"**

2. Заполните форму:
   - **Token name**: `gitlab-ci-full-access` (или любое другое имя)
   - **Expiration date**: 
     - Оставьте пустым для бессрочного токена
     - Или выберите дату (например, через 1 год)

3. **Select scopes** - выберите следующие права:
   - ✅ **`api`** - Полный доступ к API
   - ✅ **`read_user`** - Чтение информации о пользователе
   - ✅ **`read_api`** - Чтение через API
   - ✅ **`write_repository`** - Запись в репозитории
   - ✅ **`read_registry`** - Чтение из container registry
   - ✅ **`write_registry`** - Запись в container registry
   - ✅ **`admin_mode`** - Режим администратора (если нужен)

4. Нажмите **"Create personal access token"**

### Шаг 4: Скопируйте токен

⚠️ **ВАЖНО**: Токен показывается только один раз!

1. Скопируйте токен сразу после создания
2. Сохраните его в безопасном месте
3. Используйте для:
   - GitLab CI/CD переменных
   - API запросов
   - Автоматизации

---

## Способ 2: Через Rails console (автоматически)

Если у вас есть доступ к Kubernetes:

```bash
# Получить токен автоматически
./k8s/gitlab/create-access-token.sh

# Или вручную:
kubectl exec -n gitlab $(kubectl get pods -n gitlab -l app=gitlab -o jsonpath='{.items[0].metadata.name}') -- \
  gitlab-rails runner "
user = User.find_by_username('root')
token = user.personal_access_tokens.create!(
  name: 'gitlab-ci-full-access',
  scopes: ['api', 'read_user', 'read_api', 'write_repository', 'read_registry', 'write_registry', 'admin_mode'],
  expires_at: 1.year.from_now
)
puts 'Token: ' + token.token
"
```

---

## Использование токена

### В GitLab CI/CD

1. Перейдите: **Settings → CI/CD → Variables**
2. Нажмите **"Add variable"**
3. Заполните:
   - **Key**: `GITLAB_TOKEN` (или `GITLAB_PERSONAL_ACCESS_TOKEN`)
   - **Value**: Ваш токен
   - **Type**: Variable
   - ✅ **Protect variable** (рекомендуется)
   - ✅ **Mask variable** (рекомендуется)
4. Сохраните

### В скриптах

```bash
export GITLAB_TOKEN="ваш-токен-здесь"

# Проверка токена
curl -H "PRIVATE-TOKEN: $GITLAB_TOKEN" https://gyber.org/lab/api/v4/user
```

### В .gitlab-ci.yml

```yaml
variables:
  GITLAB_TOKEN: $GITLAB_PERSONAL_ACCESS_TOKEN
```

---

## Безопасность

⚠️ **ВАЖНЫЕ ПРАВИЛА**:

1. ✅ Храните токен в секретах (GitLab CI/CD Variables)
2. ❌ НЕ коммитьте токен в репозиторий
3. ✅ Используйте минимально необходимые права (scopes)
4. ✅ Устанавливайте срок действия токена
5. ✅ Регулярно ротируйте токены (меняйте раз в 3-6 месяцев)

---

## Проверка токена

После создания токена проверьте его:

```bash
# Замените YOUR_TOKEN на ваш токен
curl -H "PRIVATE-TOKEN: YOUR_TOKEN" https://gyber.org/lab/api/v4/user

# Должен вернуть информацию о пользователе root
```

---

## Быстрая ссылка

**Прямая ссылка для создания токена**: https://gyber.org/lab/-/user_settings/personal_access_tokens



