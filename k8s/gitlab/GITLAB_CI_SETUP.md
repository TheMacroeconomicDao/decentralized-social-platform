# 🔄 Настройка GitLab CI/CD для развертывания

## Текущий статус

✅ GitLab CI/CD конфигурация создана: `.gitlab-ci.yml`
✅ Коммит выполнен: `ade448c8`
✅ Образ собран и развернут вручную

## Следующие шаги для автоматизации через GitLab CI/CD

### 1. Настройка переменных в GitLab

Перейдите в GitLab: https://gyber.org/lab → Ваш проект → Settings → CI/CD → Variables

Добавьте следующие переменные:

#### Для сборки Docker образов:
- `GHCR_USERNAME` - GitHub username (TheMacroeconomicDao)
- `GHCR_TOKEN` - GitHub Personal Access Token с правами `write:packages`

#### Для развертывания в Kubernetes:
- `KUBE_CONFIG` - Содержимое `~/.kube/config` (base64 encoded)
- `KUBE_CONTEXT` - Имя контекста Kubernetes (например: `default`)

### 2. Получение токена для GitHub Container Registry

```bash
# Создать Personal Access Token на GitHub:
# Settings → Developer settings → Personal access tokens → Tokens (classic)
# Права: write:packages, read:packages
```

### 3. Настройка Kubernetes конфигурации

```bash
# Экспортировать kubeconfig в base64
cat ~/.kube/config | base64 -w 0

# Добавить в GitLab CI/CD Variables как KUBE_CONFIG
```

### 4. Структура пайплайна

Текущий `.gitlab-ci.yml` включает:

#### Stage окружение:
- `build-stage` - Сборка образа для stage
- `deploy-stage` - Развертывание в stage (автоматически при push в `stage`)

#### Production окружение:
- `build-prod` - Сборка образа для production (manual trigger)
- `deploy-prod` - Развертывание в production (manual trigger, только после build-prod)

### 5. Использование

#### Автоматический деплой на stage:
```bash
git checkout stage
git push origin stage
# Пайплайн запустится автоматически
```

#### Ручной деплой на production:
1. Перейдите в GitLab: https://gyber.org/lab → Ваш проект → CI/CD → Pipelines
2. Нажмите "Run pipeline"
3. Выберите ветку `main`
4. Запустите job `build-prod` (manual)
5. После успешной сборки запустите `deploy-prod` (manual)

### 6. Проверка пайплайна

После настройки переменных:
1. Создайте тестовый коммит в ветке `stage`
2. Push в GitLab
3. Проверьте пайплайн в CI/CD → Pipelines
4. Убедитесь, что все jobs прошли успешно

## Текущая конфигурация

Файл `.gitlab-ci.yml` уже настроен и готов к использованию после добавления переменных.

## Отличия от GitHub Actions

- ✅ Использует GitLab Runner вместо GitHub Actions
- ✅ Переменные настраиваются в GitLab UI
- ✅ Manual approval для production
- ✅ Автоматический деплой для stage

