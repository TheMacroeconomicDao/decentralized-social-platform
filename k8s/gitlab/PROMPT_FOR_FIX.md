# 🔧 Промпт для устранения проблем GitLab в Kubernetes

## Контекст проблемы

GitLab развернут в Kubernetes кластере, но недоступен по домену `gitlab.gyber.org`. Требуется провести полное исследование кластера и устранить все проблемы для обеспечения полноценного доступа к GitLab по домену.

## Текущее состояние (известная информация)

### ✅ Что работает:
- **GitLab Deployment**: Развернут в namespace `gitlab`
  - Pod: `gitlab-5bf67c77fc-9kclw` (статус: Running, Ready: True)
  - Image: `gitlab/gitlab-ce:latest`
  - Restarts: 36 (были проблемы с OOMKilled)
  - Endpoints: `10.42.2.93:80, 10.42.2.93:443, 10.42.2.93:22`

- **Service**: `gitlab` (ClusterIP: `10.43.60.199`)
  - Порты: 80/TCP, 443/TCP, 22/TCP

- **Ingress**: `gitlab-ingress` настроен
  - Host: `gitlab.gyber.org`
  - Address: `65.108.15.30`
  - Backend: `gitlab:80 (10.42.2.93:80)`
  - Ingress Class: `traefik`
  - Annotations:
    - `cert-manager.io/cluster-issuer: letsencrypt-prod`
    - `kubernetes.io/ingress.class: traefik`
    - `traefik.ingress.kubernetes.io/router.entrypoints: web,websecure`
    - `traefik.ingress.kubernetes.io/router.tls: true`

- **Traefik LoadBalancer**: 
  - Service: `traefik` в namespace `kube-system`
  - External IP: `65.108.15.30`
  - Порты: `80:32267/TCP, 443:30260/TCP`

- **GitLab сервисы**: Все компоненты запущены
  - nginx, postgresql, gitaly, gitlab-workhorse и др.

- **Root пароль**: `73/sV+RgIpHNd9Go7w0SJoTGJoLvl71P3NwUDh2nUUk=`

### ❌ Проблемы:

1. **DNS не настроен**:
   - Домен `gitlab.gyber.org` не разрешается
   - Ошибка: `lookup gitlab.gyber.org on 10.43.0.10:53: no such host`

2. **SSL сертификат не выдан**:
   - Certificate: `gitlab-tls` (READY: False)
   - Order: `gitlab-tls-1-1581066912` (STATE: pending)
   - Challenge: `gitlab-tls-1-1581066912-3298907543` (STATE: pending)
   - Причина: Let's Encrypt не может проверить домен из-за отсутствия DNS
   - Ошибка challenge: `failed to perform self check GET request 'http://gitlab.gyber.org/.well-known/acme-challenge/...': dial tcp: lookup gitlab.gyber.org on 10.43.0.10:53: no such host`

3. **Проблемы с памятью**:
   - Pod перезапускался 36 раз
   - Причина: OOMKilled (Out of Memory)
   - Resources: requests (memory: 2Gi, cpu: 1000m), limits (memory: 4Gi, cpu: 2000m)

4. **Веб-интерфейс недоступен**:
   - При обращении по домену: `ERR_NAME_NOT_RESOLVED`
   - При обращении по IP с Host header: HTTP 502 (Bad Gateway)

### Конфигурация GitLab:
```yaml
GITLAB_OMNIBUS_CONFIG: |
  external_url 'https://gitlab.gyber.org'
  gitlab_rails['gitlab_shell_ssh_port'] = 22
  nginx['listen_port'] = 80
  nginx['listen_https'] = false
```

## Задачи для исследования и исправления

### 1. Полное исследование кластера

Выполните следующие команды для сбора полной информации:

```bash
# Общее состояние GitLab
kubectl get all -n gitlab -o wide

# Детальная информация о поде
kubectl describe pod -n gitlab -l app=gitlab

# Логи GitLab (последние 100 строк)
kubectl logs -n gitlab -l app=gitlab --tail=100

# События в namespace
kubectl get events -n gitlab --sort-by='.lastTimestamp'

# Проверка ресурсов
kubectl top pod -n gitlab
kubectl describe pod -n gitlab -l app=gitlab | grep -A 10 "Limits\|Requests"

# Проверка PVC
kubectl get pvc -n gitlab
kubectl describe pvc -n gitlab

# Проверка Ingress
kubectl describe ingress -n gitlab gitlab-ingress
kubectl get ingressclass

# Проверка Traefik
kubectl get pods -n kube-system | grep traefik
kubectl describe svc -n kube-system traefik
kubectl logs -n kube-system -l app.kubernetes.io/name=traefik --tail=50

# Проверка cert-manager
kubectl get pods -n cert-manager
kubectl get clusterissuer
kubectl describe clusterissuer letsencrypt-prod
kubectl describe certificate -n gitlab gitlab-tls
kubectl describe order -n gitlab
kubectl describe challenge -n gitlab

# Проверка DNS внутри кластера
kubectl run -it --rm debug --image=busybox --restart=Never -- nslookup gitlab.gyber.org
kubectl run -it --rm debug --image=busybox --restart=Never -- dig gitlab.gyber.org

# Проверка доступности GitLab изнутри кластера
kubectl run -it --rm debug --image=curlimages/curl --restart=Never -- curl -v http://gitlab.gitlab.svc.cluster.local
kubectl exec -n gitlab -it <gitlab-pod> -- curl -v http://localhost:80

# Проверка маршрутизации Traefik
kubectl get ingressroutes -A
kubectl get middlewares -A
```

### 2. Диагностика проблем

Исследуйте следующие аспекты:

1. **DNS конфигурация**:
   - Проверьте, настроен ли DNS на уровне DNS сервера для домена `gyber.org`
   - Проверьте CoreDNS в кластере
   - Проверьте, может ли кластер резолвить внешние DNS запросы

2. **Проблемы с памятью**:
   - Проанализируйте использование памяти GitLab
   - Проверьте, достаточно ли лимитов
   - Проверьте, нет ли утечек памяти

3. **Проблемы с Traefik**:
   - Проверьте логи Traefik на наличие ошибок
   - Проверьте маршрутизацию запросов
   - Проверьте, правильно ли настроены entrypoints

4. **Проблемы с cert-manager**:
   - Проверьте статус ClusterIssuer
   - Проверьте логи cert-manager
   - Проверьте, может ли cert-manager обращаться к Let's Encrypt

5. **Проблемы с GitLab**:
   - Проверьте, полностью ли инициализирован GitLab
   - Проверьте логи на наличие ошибок
   - Проверьте доступность веб-интерфейса изнутри пода

### 3. Устранение проблем

После исследования выполните следующие действия:

1. **Настройка DNS** (если проблема в DNS):
   - Либо настроить DNS запись A: `gitlab.gyber.org -> 65.108.15.30` на DNS сервере
   - Либо настроить CoreDNS для внутреннего резолвинга (если это внутренний домен)
   - Либо использовать временное решение через /etc/hosts (только для тестирования)

2. **Исправление проблем с памятью**:
   - Увеличить лимиты памяти если необходимо
   - Оптимизировать конфигурацию GitLab
   - Проверить на утечки памяти

3. **Исправление проблем с Traefik**:
   - Проверить и исправить конфигурацию маршрутизации
   - Проверить доступность backend'а
   - Проверить SSL/TLS настройки

4. **Исправление cert-manager**:
   - После настройки DNS, пересоздать Certificate или Order
   - Проверить, что challenge может быть выполнен

5. **Проверка доступности**:
   - После исправлений проверить доступность через:
     - DNS резолвинг: `dig gitlab.gyber.org`
     - HTTP запрос: `curl -I http://gitlab.gyber.org`
     - HTTPS запрос: `curl -I https://gitlab.gyber.org`
     - Веб-браузер: открыть `https://gitlab.gyber.org`

### 4. Финальная проверка

После устранения проблем убедитесь что:

- ✅ DNS резолвится правильно
- ✅ GitLab доступен по HTTP (http://gitlab.gyber.org)
- ✅ SSL сертификат выдан и GitLab доступен по HTTPS (https://gitlab.gyber.org)
- ✅ Веб-интерфейс работает корректно
- ✅ Можно войти с root паролем
- ✅ Нет проблем с памятью (под не перезапускается)
- ✅ Все сервисы GitLab работают стабильно

## Дополнительная информация

- **Кластер**: K3s (версии могут различаться на нодах)
- **Ноды**: 
  - `canton-node-65-108-15-30` (IP: 65.108.15.30) - это IP LoadBalancer'а
  - Другие worker ноды присутствуют
- **Namespace**: `gitlab`
- **Домен**: `gitlab.gyber.org`
- **Целевой IP**: `65.108.15.30`

## Ожидаемый результат

После устранения всех проблем:
1. GitLab должен быть доступен по `https://gitlab.gyber.org`
2. SSL сертификат должен быть валидным (выдан Let's Encrypt)
3. Веб-интерфейс должен работать полностью
4. Не должно быть проблем с памятью и перезапусками
5. Все функции GitLab должны быть доступны

---

**Начните с полного исследования кластера используя команды выше, затем последовательно устраняйте найденные проблемы.**

