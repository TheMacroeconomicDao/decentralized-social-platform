# 🚀 Быстрое решение: Доступ к GitLab

## Проблема
Ошибка `ERR_NAME_NOT_RESOLVED` - домен `gitlab.gyber.org` не разрешается в DNS.

## Решение 1: Временное (для вашего компьютера)

### macOS/Linux:
```bash
sudo sh -c 'echo "65.108.15.30 gitlab.gyber.org" >> /etc/hosts'
```

### Или вручную:
1. Откройте файл `/etc/hosts` в редакторе:
   ```bash
   sudo nano /etc/hosts
   # или
   sudo vim /etc/hosts
   ```

2. Добавьте в конец файла:
   ```
   65.108.15.30 gitlab.gyber.org
   ```

3. Сохраните файл (Ctrl+O, Enter, Ctrl+X для nano)

4. Обновите страницу в браузере (Cmd+R или Ctrl+R)

5. Откройте: **http://gitlab.gyber.org** (без https, пока SSL не готов)

### Проверка:
```bash
# Проверить что запись добавлена
grep gitlab.gyber.org /etc/hosts

# Должно показать: 65.108.15.30 gitlab.gyber.org
```

## Решение 2: Постоянное (настройка DNS)

Настройте DNS запись типа **A** на вашем DNS сервере:

```
Тип: A
Имя: gitlab
Домен: gyber.org
Значение: 65.108.15.30
TTL: 300
```

После настройки:
- Подождите 5-15 минут для распространения DNS
- SSL сертификат будет выдан автоматически
- GitLab будет доступен по https://gitlab.gyber.org

## Вход в GitLab

После настройки доступа:

- **URL**: http://gitlab.gyber.org (или https:// после настройки DNS)
- **Логин**: `root`
- **Пароль**: `73/sV+RgIpHNd9Go7w0SJoTGJoLvl71P3NwUDh2nUUk=`

## Следующие шаги

1. ✅ Настройте доступ (через /etc/hosts или DNS)
2. ✅ Откройте GitLab в браузере
3. ✅ Войдите с root паролем
4. ✅ Создайте Personal Access Token
5. ✅ Сохраните токен: `echo 'токен' > k8s/gitlab/.gitlab-token`
6. ✅ Настройте переменные: `./k8s/gitlab/setup-env-now.sh`

## Удаление временной записи

После настройки DNS на сервере, удалите запись из /etc/hosts:

```bash
sudo sed -i.bak '/gitlab.gyber.org/d' /etc/hosts
```


