# 📊 Объяснение ошибок телеметрии GitLab

## Что такое телеметрия?

**Телеметрия** - это автоматический сбор статистики использования GitLab:
- Сколько пользователей
- Какие функции используются
- Производительность системы
- И т.д.

GitLab отправляет эти данные на свои серверы для улучшения продукта.

## Ошибки в логах

### 1. `POST /lab/-/collect_events 502`
- **Что это**: Попытка отправить события использования (клики, действия пользователей)
- **Почему 502**: Endpoint недоступен или отключен
- **Влияние**: ❌ НЕ влияет на работу GitLab

### 2. `POST /lab/api/v4/usage_data/track_event 502`
- **Что это**: Попытка отправить статистику использования через API
- **Почему 502**: API endpoint недоступен или отключен
- **Влияние**: ❌ НЕ влияет на работу GitLab

## Это критично?

**НЕТ!** Эти ошибки:
- ✅ Не влияют на функциональность GitLab
- ✅ Не влияют на работу репозиториев
- ✅ Не влияют на CI/CD
- ✅ Это просто предупреждения в консоли браузера

## Как отключить телеметрию?

### Через веб-интерфейс:

1. Откройте: **https://gyber.org/lab/admin/application_settings/general**
2. Прокрутите до раздела **"Usage statistics"**
3. Отключите:
   - ✅ **"Enable service ping"** (Usage Ping)
   - ✅ **"Enable version check"** (если есть)
4. Сохраните изменения

### Через API:

```bash
curl --request PUT "https://gyber.org/lab/api/v4/application/settings" \
  --header "PRIVATE-TOKEN: ваш-токен" \
  --header "Content-Type: application/json" \
  --data '{
    "usage_ping_enabled": false,
    "version_check_enabled": false
  }'
```

## Результат

После отключения:
- ✅ Ошибки 502 для телеметрии исчезнут
- ✅ GitLab перестанет пытаться отправлять статистику
- ✅ Консоль браузера будет чище

## Примечание

Эти ошибки **не мешают** работе GitLab. Вы можете:
- Игнорировать их (они не влияют на функциональность)
- Отключить телеметрию (если хотите чистую консоль)



