# 🚨 КРИТИЧЕСКИЙ ПРОМПТ: Исправление фото Michael Hypov и восстановление Navbar

## 📋 КОНТЕКСТ ПРОБЛЕМЫ

В проекте DSP (Decentralized Social Platform) обнаружены две критические проблемы:

### Проблема 1: Неправильное фото Michael Hypov
- **Текущее состояние:** В карточке команды отображается заглушка вместо реального фото Michael Hypov
- **Ожидаемое поведение:** Должно отображаться правильное фото Michael Hypov
- **Файл с данными:** `src/widgets/Team/data/teams.ts` - id: 2, `avatarSrc: '/images/teams/Michael_Hypov.jpeg'`
- **Физический файл:** `public/images/teams/Michael_Hypov.jpeg` существует (1267x1280, JPEG)
- **Требуется:** Проверить, что файл действительно содержит фото Michael Hypov, а не заглушку. Если это заглушка - найти правильное фото и заменить.

### Проблема 2: Поломанный Navbar (меню)
- **Текущее состояние:** Используется `NavbarEnhanced` с множеством ненужных эффектов (glassmorphism, сложные анимации, opacity: 0.3 по умолчанию)
- **Ожидаемое поведение:** Должен использоваться оригинальный качественный Navbar с простой, но эффективной анимацией
- **Текущий файл:** `src/widgets/Navbar/index.ts` экспортирует `NavbarEnhanced as Navbar`
- **Оригинальный файл существует:** `src/widgets/Navbar/ui/Navbar/Navbar.tsx` и `Navbar.module.scss`
- **Требуется:** Откатить Navbar к оригинальной рабочей версии

---

## 🎯 ЗАДАЧИ

### Задача 1: Исправить фото Michael Hypov

**Шаги:**
1. Проверить содержимое файла `public/images/teams/Michael_Hypov.jpeg`
2. Если это заглушка:
   - Найти правильное фото Michael Hypov
   - Заменить файл `public/images/teams/Michael_Hypov.jpeg`
   - Убедиться, что фото корректно отображается в карточке
3. Проверить, что фото отображается на сайте https://gyber.org

**Файлы для проверки:**
- `public/images/teams/Michael_Hypov.jpeg` - физический файл
- `src/widgets/Team/data/teams.ts` - данные команды (id: 2)
- `src/entities/CardMember/ui/CardMember.tsx` - компонент карточки
- `src/shared/ui/SafeImage/SafeImage.tsx` - компонент для безопасной загрузки изображений

---

### Задача 2: Восстановить оригинальный Navbar

**Шаги:**
1. Изучить текущий `NavbarEnhanced` и понять, что было добавлено лишнего
2. Изучить оригинальный `Navbar.tsx` и `Navbar.module.scss`
3. Найти в истории Git коммит, где Navbar был рабочим и качественным
4. Восстановить оригинальный Navbar:
   - Изменить `src/widgets/Navbar/index.ts` чтобы экспортировать оригинальный `Navbar` вместо `NavbarEnhanced`
   - Убедиться, что стили в `Navbar.module.scss` корректны
   - Проверить, что все анимации работают правильно
   - Убедиться, что адаптивность сохранена

**Ключевые файлы:**
- `src/widgets/Navbar/index.ts` - точка экспорта (сейчас экспортирует Enhanced)
- `src/widgets/Navbar/ui/Navbar/Navbar.tsx` - оригинальный компонент
- `src/widgets/Navbar/ui/Navbar/Navbar.module.scss` - оригинальные стили
- `src/widgets/Navbar/ui/Navbar/Navbar-Enhanced.tsx` - поломанная версия (НЕ ИСПОЛЬЗОВАТЬ)
- `src/widgets/Navbar/ui/Navbar/Navbar-Enhanced.module.scss` - поломанные стили (НЕ ИСПОЛЬЗОВАТЬ)

**История коммитов для проверки:**
```bash
git log --oneline --all -- src/widgets/Navbar/ | head -20
git show d9540c4b:src/widgets/Navbar/index.ts  # Проверить старую версию
```

**Что должно быть в оригинальном Navbar:**
- Простая градиентная линия сверху
- Золотой индикатор активной страницы (hatLink)
- Плавные анимации без излишеств
- Полная видимость по умолчанию (не opacity: 0.3)
- Корректная адаптивность для всех разрешений

---

## ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА

### НЕЛЬЗЯ:
- ❌ Использовать NavbarEnhanced или любые Enhanced версии
- ❌ Добавлять glassmorphism эффекты
- ❌ Делать Navbar полупрозрачным по умолчанию
- ❌ Удалять оригинальные файлы Navbar.tsx и Navbar.module.scss
- ❌ Менять структуру данных команды (teams.ts)
- ❌ Ломать существующие анимации

### МОЖНО:
- ✅ Восстановить оригинальный Navbar из истории Git
- ✅ Исправить только реальные проблемы с адаптивностью
- ✅ Заменить фото Michael Hypov на правильное
- ✅ Оптимизировать производительность без изменения дизайна

---

## 📁 СТРУКТУРА ФАЙЛОВ

```
src/
├── widgets/
│   ├── Navbar/
│   │   ├── index.ts                    # ← ИЗМЕНИТЬ: экспортировать оригинальный Navbar
│   │   ├── data/
│   │   │   └── routesData.ts
│   │   └── ui/
│   │       ├── Navbar/
│   │       │   ├── Navbar.tsx          # ← ИСПОЛЬЗОВАТЬ: оригинальный компонент
│   │       │   ├── Navbar.module.scss  # ← ИСПОЛЬЗОВАТЬ: оригинальные стили
│   │       │   ├── Navbar-Enhanced.tsx # ← НЕ ИСПОЛЬЗОВАТЬ
│   │       │   └── Navbar-Enhanced.module.scss # ← НЕ ИСПОЛЬЗОВАТЬ
│   │       ├── NavbarMobile/
│   │       └── NavbarTablet/
│   └── Team/
│       ├── data/
│       │   └── teams.ts                # ← ПРОВЕРИТЬ: данные Michael Hypov (id: 2)
│       └── ui/
│           └── Team.tsx
├── entities/
│   └── CardMember/
│       └── ui/
│           └── CardMember.tsx          # ← ПРОВЕРИТЬ: отображение фото
└── shared/
    └── ui/
        └── SafeImage/
            └── SafeImage.tsx            # ← ПРОВЕРИТЬ: обработка ошибок загрузки

public/
└── images/
    └── teams/
        └── Michael_Hypov.jpeg          # ← ПРОВЕРИТЬ/ЗАМЕНИТЬ: фото
```

---

## 🔍 ПРОВЕРОЧНЫЙ ЧЕКЛИСТ

### После исправления фото:
- [ ] Файл `public/images/teams/Michael_Hypov.jpeg` содержит правильное фото (не заглушку)
- [ ] Фото отображается в карточке на локальной версии
- [ ] Фото отображается на https://gyber.org
- [ ] Размеры фото корректны (не искажены)
- [ ] Fallback на placeholder работает при ошибке загрузки

### После восстановления Navbar:
- [ ] `src/widgets/Navbar/index.ts` экспортирует оригинальный `Navbar`
- [ ] Navbar полностью видимый по умолчанию (не полупрозрачный)
- [ ] Градиентная линия отображается корректно
- [ ] Золотой индикатор активной страницы работает
- [ ] Анимации плавные и не перегружены эффектами
- [ ] Адаптивность работает на всех разрешениях:
  - Desktop (> 1024px): Navbar видим
  - Tablet (768px - 1024px): Navbar видим
  - Mobile (< 900px): Navbar скрыт, используется NavbarMobile
- [ ] Все ссылки работают корректно
- [ ] Активная страница подсвечивается правильно

---

## 🧪 ТЕСТИРОВАНИЕ

### Локальное тестирование:
```bash
npm run dev
```

Проверить:
1. Открыть http://localhost:3000
2. Проверить, что Michael Hypov отображается с правильным фото
3. Проверить, что Navbar работает корректно:
   - Видимый по умолчанию
   - Плавные анимации
   - Корректное переключение между страницами
   - Адаптивность на разных разрешениях

### Production тестирование:
После деплоя проверить на https://gyber.org:
1. Фото Michael Hypov корректно
2. Navbar работает как ожидается

---

## 📝 КОМАНДЫ ДЛЯ ВОССТАНОВЛЕНИЯ

### Проверка истории Navbar:
```bash
# Найти коммиты с изменениями Navbar
git log --oneline --all -- src/widgets/Navbar/ | head -20

# Посмотреть старую версию index.ts
git show d9540c4b:src/widgets/Navbar/index.ts

# Посмотреть оригинальный Navbar.tsx из старого коммита
git show d9540c4b:src/widgets/Navbar/ui/Navbar/Navbar.tsx

# Посмотреть оригинальные стили
git show d9540c4b:src/widgets/Navbar/ui/Navbar/Navbar.module.scss
```

### Восстановление оригинального Navbar:
```bash
# Вариант 1: Изменить index.ts вручную
# Заменить:
# import { NavbarEnhanced as Navbar } from "./ui/Navbar/Navbar-Enhanced";
# На:
# import { Navbar } from "./ui/Navbar/Navbar";

# Вариант 2: Восстановить из старого коммита (если нужно)
# git checkout d9540c4b -- src/widgets/Navbar/index.ts
```

---

## ✅ КРИТЕРИИ УСПЕХА

### Фото Michael Hypov:
- ✅ Правильное фото отображается (не заглушка)
- ✅ Фото видно на сайте
- ✅ Нет ошибок загрузки изображения

### Navbar:
- ✅ Оригинальный Navbar восстановлен
- ✅ Navbar полностью видимый по умолчанию
- ✅ Все анимации работают плавно
- ✅ Адаптивность корректна на всех устройствах
- ✅ Нет лишних эффектов (glassmorphism, избыточные анимации)
- ✅ Производительность не ухудшена

---

## 🚀 ПОСЛЕ ИСПРАВЛЕНИЯ

1. Закоммитить изменения:
```bash
git add .
git commit -m "fix: восстановлен оригинальный Navbar и исправлено фото Michael Hypov"
```

2. Собрать Docker образ:
```bash
COMMIT_HASH=$(git rev-parse --short HEAD)
docker build -t ghcr.io/themacroeconomicdao/decentralized-social-platform/dsp-prod:$COMMIT_HASH -t ghcr.io/themacroeconomicdao/decentralized-social-platform/dsp-prod:latest -f Dockerfile .
```

3. Запушить в registry и обновить deployment:
```bash
docker push ghcr.io/themacroeconomicdao/decentralized-social-platform/dsp-prod:$COMMIT_HASH
docker push ghcr.io/themacroeconomicdao/decentralized-social-platform/dsp-prod:latest
kubectl set image deployment/dsp-prod-deployment-primary dsp-prod=ghcr.io/themacroeconomicdao/decentralized-social-platform/dsp-prod:$COMMIT_HASH -n default
kubectl rollout status deployment/dsp-prod-deployment-primary --timeout=600s -n default
```

4. Проверить на production:
- https://gyber.org - проверить фото Michael Hypov
- https://gyber.org - проверить работу Navbar

---

**ВАЖНО:** Работать аккуратно, не ломать существующий функционал. Восстановить оригинальный качественный Navbar, а не создавать новую версию.


