# 🎨 Промпт: Проверка и улучшение верстки и адаптивности DSP

## 📋 ЗАДАЧА

Провести полный аудит и улучшение верстки и адаптивности проекта DSP (Decentralized Social Platform), привести код к best practices, очистить мусор, при этом **НЕ СЛОМАТЬ** существующий функционал и **НЕ ПОТЕРЯТЬ** контент.

---

## 🎯 ОСНОВНЫЕ ТРЕБОВАНИЯ

### 1. Анализ текущего состояния
- Проанализировать структуру проекта (Next.js 15.1.8, App Router)
- Проверить все компоненты на адаптивность (mobile, tablet, desktop)
- Найти неиспользуемые стили, компоненты, зависимости
- Выявить проблемы с версткой (overflow, z-index, positioning)
- Проверить accessibility (a11y) стандарты

### 2. Адаптивность (Responsive Design)
- **Mobile First подход** - все стили должны начинаться с mobile
- Проверить breakpoints:
  - Mobile: 320px - 767px
  - Tablet: 768px - 1023px
  - Desktop: 1024px+
- Использовать CSS Grid и Flexbox правильно
- Проверить все медиа-запросы на корректность
- Убедиться, что все изображения responsive
- Проверить touch targets (минимум 44x44px на mobile)

### 3. Очистка мусора
- Удалить неиспользуемые CSS классы и стили
- Удалить неиспользуемые компоненты
- Удалить неиспользуемые зависимости из package.json
- Удалить дублирующийся код
- Очистить неиспользуемые импорты
- Удалить закомментированный код (если не критично)

### 4. Best Practices
- Использовать CSS Modules правильно
- Применить BEM методологию где уместно
- Оптимизировать CSS (убрать дублирование)
- Использовать CSS переменные для цветов, размеров, breakpoints
- Правильная работа с изображениями (next/image)
- Оптимизация шрифтов (font-display, preload)
- Правильная работа с анимациями (performance)

### 5. Критичные компоненты для проверки
- `src/app/layout.tsx` - основной layout
- `src/widgets/Navbar/` - навигация (должна быть адаптивной)
- `src/widgets/Team/` - список команды (grid адаптивный)
- `src/widgets/Footer/` - футер
- `src/entities/CardMember/` - карточки участников
- `src/app/page.tsx` - главная страница
- Все страницы в `src/app/` (aic, platform, experiment, unit-profile, etc.)

---

## 🔍 ЧЕКЛИСТ ПРОВЕРКИ

### Верстка
- [ ] Нет горизонтального скролла на любых разрешениях
- [ ] Все элементы видны и доступны на mobile
- [ ] Текст читаемый на всех устройствах (размер шрифта, контраст)
- [ ] Изображения не выходят за границы контейнеров
- [ ] Формы работают корректно на mobile
- [ ] Кнопки и ссылки имеют достаточный размер для touch
- [ ] Модальные окна адаптивны
- [ ] Таблицы (если есть) адаптивны или имеют горизонтальный скролл

### CSS/SCSS
- [ ] Используются CSS переменные для цветов и размеров
- [ ] Нет дублирования стилей
- [ ] Медиа-запросы организованы правильно
- [ ] Нет !important без необходимости
- [ ] Используется правильная специфичность
- [ ] CSS Modules используются корректно
- [ ] Нет inline стилей (кроме динамических)

### Компоненты
- [ ] Все компоненты адаптивны
- [ ] Нет жестко заданных размеров (px вместо rem/em где возможно)
- [ ] Используется clamp() для fluid typography
- [ ] Правильная работа с overflow
- [ ] Правильное использование z-index
- [ ] Анимации оптимизированы (will-change, transform)

### Производительность
- [ ] Нет неиспользуемых CSS
- [ ] Критический CSS inline
- [ ] Lazy loading для изображений
- [ ] Оптимизация шрифтов
- [ ] Минификация CSS в production

---

## 🛠️ ПРОЦЕСС РАБОТЫ

### Шаг 1: Анализ
1. Изучить структуру проекта
2. Найти все компоненты и их стили
3. Проверить адаптивность в браузере (DevTools)
4. Составить список проблем

### Шаг 2: Очистка
1. Удалить неиспользуемые файлы и код
2. Очистить зависимости
3. Удалить дублирующийся код
4. Оптимизировать импорты

### Шаг 3: Улучшение
1. Применить best practices
2. Исправить проблемы адаптивности
3. Оптимизировать CSS
4. Улучшить accessibility

### Шаг 4: Тестирование
1. Проверить на разных устройствах (Chrome DevTools)
2. Проверить все страницы
3. Убедиться, что ничего не сломано
4. Проверить производительность

---

## ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА

### НЕЛЬЗЯ:
- ❌ Удалять используемый функционал
- ❌ Менять структуру данных
- ❌ Ломать существующие анимации
- ❌ Менять цветовую схему без необходимости
- ❌ Удалять важные комментарии
- ❌ Менять API интерфейсы компонентов
- ❌ Удалять используемые зависимости

### МОЖНО:
- ✅ Улучшать стили и адаптивность
- ✅ Оптимизировать CSS
- ✅ Удалять неиспользуемый код
- ✅ Рефакторить дублирующийся код
- ✅ Улучшать accessibility
- ✅ Оптимизировать производительность

---

## 📁 СТРУКТУРА ПРОЕКТА

```
src/
├── app/                    # Next.js App Router страницы
│   ├── layout.tsx         # Главный layout
│   ├── page.tsx           # Главная страница
│   └── [другие страницы]
├── widgets/               # Виджеты (большие компоненты)
│   ├── Navbar/           # Навигация
│   ├── Team/             # Команда
│   ├── Footer/           # Футер
│   └── [другие виджеты]
├── entities/             # Бизнес-сущности
│   └── CardMember/       # Карточка участника
├── shared/               # Общие компоненты и утилиты
│   ├── ui/               # UI компоненты
│   └── lib/              # Утилиты
└── features/             # Фичи
```

---

## 🎨 СТИЛИ И ПОДХОДЫ

### CSS Modules
- Используются для всех компонентов
- Имена классов в camelCase
- Пример: `CardMember.module.scss`

### Breakpoints (рекомендуемые)
```scss
$mobile: 320px;
$tablet: 768px;
$desktop: 1024px;
$wide: 1440px;

// Mobile First
@media (min-width: $tablet) { ... }
@media (min-width: $desktop) { ... }
```

### CSS Variables (рекомендуемые)
```scss
:root {
  --color-primary: #d49d32;
  --color-secondary: #42b8f3;
  --spacing-xs: 0.5rem;
  --spacing-sm: 1rem;
  --spacing-md: 2rem;
  --spacing-lg: 4rem;
  --breakpoint-tablet: 768px;
  --breakpoint-desktop: 1024px;
}
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Устройства для проверки:
- iPhone SE (375px)
- iPhone 12/13 (390px)
- iPhone 14 Pro Max (430px)
- iPad (768px)
- iPad Pro (1024px)
- Desktop (1920px)

### Браузеры:
- Chrome (основной)
- Safari (iOS)
- Firefox
- Edge

---

## 📝 ОТЧЕТ

После выполнения работы предоставить:

1. **Список изменений:**
   - Что было исправлено
   - Что было удалено
   - Что было оптимизировано

2. **Проблемы, которые были найдены:**
   - Критичные
   - Важные
   - Незначительные

3. **Рекомендации:**
   - Что можно улучшить в будущем
   - Best practices для поддержки

---

## 🚀 КОМАНДЫ ДЛЯ ПРОВЕРКИ

```bash
# Запуск dev сервера
npm run dev

# Проверка линтера
npm run lint

# Проверка типов
npm run type-check

# Сборка для проверки
npm run build
```

---

## 💡 ПРИМЕРЫ ПРОБЛЕМ И РЕШЕНИЙ

### Проблема: Горизонтальный скролл на mobile
**Решение:** Добавить `overflow-x: hidden` на body, проверить все элементы на `width: 100vw` и заменить на `width: 100%`

### Проблема: Текст слишком мелкий на mobile
**Решение:** Использовать `clamp(14px, 4vw, 18px)` для адаптивной типографики

### Проблема: Кнопки слишком маленькие для touch
**Решение:** Минимум `min-height: 44px; min-width: 44px;` для интерактивных элементов

### Проблема: Изображения выходят за границы
**Решение:** Использовать `max-width: 100%; height: auto;` или `next/image` компонент

---

## ✅ КРИТЕРИИ УСПЕХА

- ✅ Нет горизонтального скролла на любых устройствах
- ✅ Все элементы доступны и видны на mobile
- ✅ Производительность улучшена (меньше CSS, оптимизация)
- ✅ Код очищен от мусора
- ✅ Применены best practices
- ✅ Ничего не сломано (все функции работают)
- ✅ Контент не потерян

---

**ВАЖНО:** Работать аккуратно, тестировать каждое изменение, не удалять ничего без проверки использования!


